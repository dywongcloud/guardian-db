//! Integration tests proving realtime `postgres_changes` delivery for
//! replicated (remote-authored) row changes, not just local commits.
//!
//! Placeholder pending the replicated-realtime implementation phase.

#[test]
fn placeholder() {}
